# async HTTP in Python

httpx and aiohttp are async. Good news: requests supports async natively since 2.0, so you can await it directly. Sync code also works. Source: https://requests.readthedocs.io/
